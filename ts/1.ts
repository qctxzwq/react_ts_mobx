let a: string = "123"
enum Color { "red", "green", "bule" }
let color: Color = Color.bule

/**
 * 
 * @param a 加数1
 * @param b 加数2
 * @returns 返回结果
 */
const fn1 = (a: number, b: number): number => {
  return a + b
}
console.log(color);
fn1(123,12)


export default void 0